plugins {
    id 'java'
    id 'io.papermc.paperweight.userdev' version '2.0.0-beta.21'
    id 'com.gradleup.shadow' version '9.4.2'
}

group = 'com.mcplugin'
version = '1.7.58'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(25)
    }
}

repositories {
    mavenCentral()
    maven {
        url = 'https://repo.papermc.io/repository/maven-public/'
    }
}

dependencies {
    paperweight.paperDevBundle("26.2.build.+")
    implementation 'org.xerial:sqlite-jdbc:3.46.1.3'
    implementation 'de.mkammerer:argon2-jvm:2.11'
}

tasks {
    assemble {
        dependsOn(shadowJar)
    }

    shadowJar {
        doLast {
            def jarDir = new File(project.projectDir, "Jar")
            def jarName = "MC-Plugin-${project.version}.jar"
            def source = shadowJar.archiveFile.get().asFile

            if (!source.exists()) return

            // Создаём папку Jar/, если её нет
            jarDir.mkdirs()

            // Полностью очищаем папку Jar/ от всех файлов
            jarDir.listFiles().each { f ->
                if (f.delete()) {
                    logger.lifecycle("[JAR] Удалён: ${f.name}")
                }
            }

            // Копируем свежий fat jar через Java NIO
            def target = new File(jarDir, jarName)
            java.nio.file.Files.copy(
                source.toPath(),
                target.toPath(),
                java.nio.file.StandardCopyOption.REPLACE_EXISTING
            )
            logger.lifecycle("[JAR] Скопирован в Jar/: ${jarName}")
        }
    }
}

// Paper 26.x (1.21.4+) uses Mojang mappings natively - no reobf needed
// The dev bundle does not provide reobf mappings for modern Paper
tasks.configureEach {
    if (name == "reobfJar") {
        enabled = false
    }
}